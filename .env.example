# Template only. For local Cloudflare Worker bindings, copy to .dev.vars.
# Optional shared fal.ai API key; otherwise each user connects their own key.
FAL_KEY=
# Required to encrypt saved per-user keys. Use a new random secret for development.
# Keep production's existing value in Sites; never copy it into this repository.
KEY_ENCRYPTION_SECRET=
